---
olaresManifest.version: '0.12.0'
olaresManifest.type: app
apiVersion: 'v3'

workloadReplicas:
  aimomnivoice: 1

metadata:
  name: aimomnivoice
  appid: aimomnivoice
  icon: https://raw.githubusercontent.com/bayerhazard/aimomnivoice/main/icon.png
  description: OmniVoice 0.6B TTS — zero-shot voice cloning, voice design, 600+ languages, OpenAI-compatible API
  title: AIM OmniVoice 0.6B TTS
  version: '26.08.7'
  categories:
    - Audio

entrances:
  - name: aimomnivoice
    port: 8080
    host: aimomnivoice
    title: AIM OmniVoice 0.6B TTS
    icon: https://raw.githubusercontent.com/bayerhazard/aimomnivoice/main/icon.png
    openMethod: window
    authLevel: internal

spec:
  versionName: '26.08.7'
  upgradeDescription: |
    v26.08.7: Dashboard-Branding "AImighty OmniVoice TTS" (AI in Gold), Karten-Titel "Was soll ich sagen?", App-Icon wie AIM Voxtral 4B TTS. plus v26.08.6: Dashboard-Relaunch — neues Layout (Text oben, Stimme + Voice Cloning nebeneinander, "Sprachausgabe generieren"), Mikrofon-Aufnahme für Referenz-Audio, Named Clones im UI (Dropdown + "Als Klon speichern"), Designguide-Konformität (Geist selbst gehostet, Hanseatenblau/Gold, keine Gradienten). plus v26.08.5: Voice-Clone-Registry supports WAV/MP3/M4A reference audio (extension-preserving) — plus v26.08.4: Voice-Clone-Registry (POST/GET/DELETE /v1/audio/clones) + v26.08.3: Named voice clones (voice: "clone:<name>", Referenz-Audio aus voices/) — basiert auf 26.08.2: OmniVoice 0.6B zero-shot TTS via native omnivoice runtime (FastAPI wrapper, digest-pinned diogod2r/omnivoice-fastapi). Endpoints: /v1/audio/speech (OpenAI-compatible, presets + free-form instruct), /v1/audio/design, /v1/audio/clone, /v1/models, /v1/voices. WAV->MP3 transcode sidecar for LiteLLM/Open WebUI compatibility. ~4 GB VRAM on worker GPU (MemorySlice co-residency). Built for Olares 1.12.6.
  fullDescription: |
    **OmniVoice-0.6B** — k2-fsa's massively multilingual zero-shot TTS (600+ languages) based on a Qwen3-0.6B diffusion language model.

    **Model**
    k2-fsa/OmniVoice (~2.5 GB fp16), trained on 581k hours of open-source speech data. Apache-2.0 licensed.

    **Inference Engine**
    Native omnivoice runtime (PyTorch 2.8 cu128) behind an OpenAI-compatible FastAPI wrapper. Single process, requests queued per device.

    **Key Features**
    - OpenAI-compatible API: /v1/audio/speech, /v1/models, /v1/voices
    - Zero-shot voice cloning from 3-10 s reference audio: POST /v1/audio/clone
    - Voice design via attributes: "female, low pitch, british accent"
    - Preset voices (auto, female, male, child, whisper, ...) mapped to design instructs
    - Non-verbal tags like [laughter], pronunciation control via phonemes/pinyin
    - 24 kHz output; WAV->MP3 transcode sidecar for LiteLLM/Open WebUI compatibility
    - Web dashboard for quick testing (synthesis + health)

    **API Example**
    ```bash
    curl -X POST http://localhost:8880/v1/audio/speech \
      -H "Content-Type: application/json" \
      -d '{"model": "omnivoice", "input": "Hallo Welt!", "voice": "female"}' \
      -o output.wav
    ```

    **Resource Usage**
    GPU: 1× NVIDIA (~4 GB VRAM)
    RAM: 6-16 GB, CPU: 2-8 cores
    Disk: 20 GB (model cache)

    **Note:** Apache-2.0 — free for personal and commercial use. Only clone voices you have the rights and consent to clone.
  developer: Aimighty
  website: https://github.com/bayerhazard/aimomnivoice
  sourceCode: https://github.com/bayerhazard/aimomnivoice
  locale:
    - en-US
  supportArch:
    - amd64
  requiredCpu: "2"
  limitedCpu: "8"
  requiredMemory: 6Gi
  limitedMemory: 16Gi
  requiredDisk: 20Gi
  limitedDisk: 50Gi
  requiredGpu: 1

permission:
  appData: true

envs:
  - envName: HF_TOKEN
    required: false
    applyOnChange: true
    valueFrom:
      envName: OLARES_USER_HUGGINGFACE_TOKEN

options:
  apiTimeout: 0
  allowMultipleInstall: false
  dependencies:
    - name: olares
      version: '>=1.12.6-0'
      type: system
